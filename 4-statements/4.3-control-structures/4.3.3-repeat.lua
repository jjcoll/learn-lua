-- Repeat until statement repeats its body unti its condition is true. The test is done after the body,so the body
-- is always executed at least once. 

-- print the first non-empty line
password = 22
i = 00
repeat
  i = i + 1 
until password == i
print(i)
